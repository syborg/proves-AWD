<div class="cart-title">Your Pragmatic Cart</div>
<table>
  <%= render :partial=>'cart_item', :collection=>cart.items %>
  <tr class="total-line">
    <td colspan="2">Total</td>
    <td class="total-cell"><%= number_to_currency(@cart.total_price, :format=>"%n%u", :unit=>'€') %> </td>
  </tr>
</table>

<%= button_to I18n.t('layout.cart.button.checkout'), :action=>:checkout %>
<%= button_to I18n.t('layout.cart.button.empty'), :action=>:empty_cart %>
